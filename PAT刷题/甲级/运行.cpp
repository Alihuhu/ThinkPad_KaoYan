#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<vector<int>> array;
    array[0].push_back(0);
    array[1].push_back(1);
    array[1].push_back(2);
    array[2].push_back(3);
    printf("%d",array[0].size());
}
